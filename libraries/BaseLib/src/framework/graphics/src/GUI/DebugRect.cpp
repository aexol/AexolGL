#include "aex/gui/DebugRect.hpp"

namespace aex
{
    namespace gui
    {
        DebugRect::DebugRect(const Area& area)
        {
            setArea(area);
        }

        DebugRect::~DebugRect()
        {
        }

        void DebugRect::refresh(int flags)
        {

        }

        void DebugRect::draw(AexRenderer* gl)
        {

        }

        void DebugRect::finishdraw(AexRenderer* gl)
        {

        }

        void DebugRect::needsUpdate()
        {

        }
    }
}